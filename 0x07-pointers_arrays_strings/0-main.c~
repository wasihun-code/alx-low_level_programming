#include "holberton.h"
#include <stdio.h>

int main(void)
{
	char buffer[98] = {0x00};

	simple_print_buffer(buffer, 98);
	_memset(buffer, 0x01, 95);
	printf("----------------------\n");
	simle_print_buffer(buffer, 98);
	return (0);
}
