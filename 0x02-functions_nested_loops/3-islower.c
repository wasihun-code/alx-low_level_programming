#include "holberton.h"
#include <ctype.h>

/**
 * _islower - documentation
 * Return: 0
 */

int _islower(int c)
{
	int results;

	results = _islower(c);
	if (results >= 97 && results <= 122)
	{
		results == 1;
	}
	else
	{
		results == 0;
	}
	return (results);
}
