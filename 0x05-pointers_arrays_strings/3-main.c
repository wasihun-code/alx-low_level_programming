#include "holberton.h"

/**
 * main -check the code
 * Return: 0.
 */

int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Issac Asimov";
	_puts(str);
	return (0);
}
