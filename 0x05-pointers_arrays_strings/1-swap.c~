#include "holberton.h"
#include <stdio.h>

/**
 * swap_int - swaps two integers.
 * @swap: store value of a.
 * Return: 0 since void.
 */

void swap_int(int *a, int *b)
{
	int temp;

	temp = *a;
	*a = *b;
	*b = temp;
}
