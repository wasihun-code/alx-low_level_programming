#include "holberton.h"

/**
 * main - check the code for alx school students.
 * Return: Always 0.
 */

int main(void)
{
	print_square(0);
	print_square(2);
	print_square(10);
	print_square(-4);
	return (0);
}
