#include <stdio.h>
#include <stdlib.h>

/**
 * main: numbers
 * main - numbers
 * Return: numbers
 */

int main(void)
{
	int i = 0;
	while (i < 10)
	{
		putchar(i);
		i++;
	}
	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
