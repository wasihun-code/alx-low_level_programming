#include "holberton.h"

/**
 * wildcmp - compares two strings and returns one or zero.
 * @s1: string 1.
 * @s2: string 2.
 * Return: 0.
 */

int wildcmp(char *s1, char *s2)
{
	int i, j = 0, k = 0;

	for (i = 0; s1[j] != '\0'; i++, j++)
		;

	while 
}